import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titleBox: {
        alignItems: "center"
    },
    title: {
        fontSize: 50,
        fontWeight: "bold"
    }
})

export default styles;